{
  "name": "telegram-crm-surveys",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "echo 'stub build';",
    "dev": "concurrently -n bot,worker,admin -c auto \"pnpm --filter @apps/bot dev\" \"pnpm --filter @packages/queue dev\" \"pnpm --filter @apps/admin dev\"",
    "lint": "eslint . --ext .ts,.tsx || true",
    "test": "vitest run",
    "db:migrate": "pnpm --filter @packages/db db:migrate"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "eslint": "^9.0.0",
    "vitest": "^2.0.0",
    "typescript": "^5.4.0"
  }
}